server {
    listen 3000;

    root /usr/share/nginx/html;
    index index.html;

    # Auction WebSocket (notification service)
    location ~ ^/auctions/[^/]+/subscribe {
        proxy_pass         http://notification:8080;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
    }

    # User notification WebSocket (notification service)
    location /notifications/subscribe {
        proxy_pass         http://notification:8080;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
    }

    # User notifications REST (notification service)
    location /notifications {
        proxy_pass http://notification:8080;
    }

    # Metrics (notification service)
    location /metrics {
        proxy_pass http://notification:8080;
    }

    # User payments (payment service — must precede /users)
    location ~ ^/users/[^/]+/payments {
        proxy_pass http://payment:8085;
    }

    # Auction payment (payment service — must precede /auctions)
    location ~ ^/auctions/[^/]+/payment$ {
        proxy_pass http://payment:8085;
    }

    # Auth + Users (user service)
    location /auth {
        proxy_pass http://user:8082;
    }

    location /users {
        proxy_pass http://user:8082;
    }

    # Shop auctions (auction service — must precede /shops)
    location ~ ^/shops/[^/]+/auctions {
        proxy_pass http://auction:8081;
    }

    # Shops + Items + Sellers (shop service)
    location /shops {
        proxy_pass http://shop:8083;
    }

    location /items {
        proxy_pass http://shop:8083;
    }

    location /sellers {
        proxy_pass http://shop:8083;
    }

    # Bids (bid service)
    location /bids {
        proxy_pass http://bid:8084;
    }

    # Auctions (auction service)
    location /auctions {
        proxy_pass http://auction:8081;
    }

    # Block browser metadata requests from hitting the SPA fallback
    location /.well-known/ {
        return 404;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
